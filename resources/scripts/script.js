var $w = $(window).scroll(function(){  if (document.getElementsByClassName("home")[0]) {    if ( $w.scrollTop() > $(".section:eq(0)").offset().top + 100) {      $('.navbar').addClass("fixed");    } else {      $('.navbar').removeClass("fixed");    }  }});$(".menu-icon").click(function() {    if (document.getElementsByClassName("menu-icon active")[0]) {        $(".menu-icon").removeClass("active");        $(".fullnav").removeClass("active");        $(".navbar").removeClass("menu-active");        $(".custom-cursor").removeClass("white");    } else {        $(".menu-icon").addClass("active");        $(".fullnav").addClass("active");        $(".navbar").addClass("menu-active");        $(".custom-cursor").addClass("white");    }});$(".scroll-down-btn").click(function(e) {    e.preventDefault();    var aid = $(this).attr("href");    $('html,body').animate({scrollTop: $(aid).offset().top},'slow');});$(function(){    jQuery('.image-wrapper .icon').each(function(){        var $img = jQuery(this);        var imgID = $img.attr('id');        var imgClass = $img.attr('class');        var imgURL = $img.attr('src');            jQuery.get(imgURL, function(data) {            var $svg = jQuery(data).find('svg');                if(typeof imgID !== 'undefined') {                $svg = $svg.attr('id', imgID);            }            if(typeof imgClass !== 'undefined') {                $svg = $svg.attr('class', imgClass+' replaced-svg');            }                $svg = $svg.removeAttr('xmlns:a');                        if(!$svg.attr('viewBox') && $svg.attr('height') && $svg.attr('width')) {                $svg.attr('viewBox', '0 0 ' + $svg.attr('height') + ' ' + $svg.attr('width'))            }                $img.replaceWith($svg);            }, 'xml');        });});